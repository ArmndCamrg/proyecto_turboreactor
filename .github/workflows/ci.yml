name: CI

permissions: read-all

on:
  pull_request:
  push:
    branches:
      - develop

jobs:
  basic-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Basic validation
        run: echo "Add project-specific lint/test/type checks here"