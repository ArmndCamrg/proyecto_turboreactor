name: Terraform Plan

permissions: read-all

on:
  pull_request:
    paths:
      - 'infra/**'

jobs:
  terraform-plan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Terraform plan placeholder
        run: echo "Add terraform fmt, validate, and plan here"