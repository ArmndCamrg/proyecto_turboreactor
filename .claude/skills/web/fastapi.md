# FastAPI Skill

## Rules
- separate routers, services, schemas, and config
- validate inputs
- keep environment config centralized
- include health endpoint when appropriate
- document local run and container run