# /ship-dev

Use this command to prepare a dev deployment safely.

## Checks before deploy
- current branch is correct
- working tree is in expected state
- required checks passed
- required environment variables exist
- target project and region are explicit
- deployment is for dev only

## Output
- deployment summary
- commands to run
- risks
- rollback note

Do not deploy automatically without approval.